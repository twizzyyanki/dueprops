site-header
  //- header.jade
main(layout="horizontal" layout-fill)
  material-sidenav(flex layout="horizontal" style="padding-top: 10px" component-id="left")
    material-content.propgrid
      .prop(ng-repeat="prop in props")
        img(ng-click="openPropDialog(prop)" ng-src="{{prop.icon}}" class="prop icon")

  material-content.feed(layout="horizontal" style="align-items: flex-start; align-content: flex-start")
    prop-card(ng-repeat="prop in feed")
      //- prop-card.jade

footer
  | Copyright &copy; 2010-2014 DueProps.com
  | &nbsp;∙&nbsp;
  a(href="mailto:support@dueprops.com") Support
